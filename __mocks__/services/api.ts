import { jest } from '@jest/globals';

export const getUserDetails = jest.fn();
export const getUserPosts = jest.fn();
export const getUserTodos = jest.fn();
export const getEnrichedUsers = jest.fn();
